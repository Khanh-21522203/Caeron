- When a user asks to review a subsystem and later clarifies they want "all issues," do not limit the final answer to only top findings. Run a full file-by-file audit, separate confirmed implementation bugs from test-quality gaps, and explicitly state any areas that remain uncertain.
- For code review requests in this repo, default to a comprehensive pass unless the user explicitly asks for a quick/high-priority review. Do not make the user restate "all issues"; include lower-severity correctness risks and test gaps in the first response.
- Before restating prior review findings, re-open the current files and verify each finding still exists. Do not repeat stale findings after the user says fixes landed; treat that as a correction and re-audit against the current working tree.
